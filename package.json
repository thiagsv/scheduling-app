{
  "name": "scheduling-app",
  "version": "1.0.0",
  "description": "AllieHealth Scheduling App",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  }
}
